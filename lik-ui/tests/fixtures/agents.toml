# Test roster used by the chat/route tests. Not the shipped roster. Name-based: the fake agents
# client resolves these names to agent_1 / env_1 at startup (see FakeAgentsClient in test_agents.py).
default_environment = "Test Env"

[[sections]]
name = "Knowledge"

[[agents]]
agent = "Test Agent"
section = "Knowledge"
user_prompt = "Ask the Test Agent about test things."
